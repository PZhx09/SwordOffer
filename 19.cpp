#include<iostream>
#include<string>
using namespace std;

class Solution {
public:
    bool isMatch(string s, string p) {
        int i = 0, j = 0;
        if (p[1] == '*')
        {

        }

        while (i != s.length() || j != p.length())
        {
        }
    }
};